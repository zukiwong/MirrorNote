import SwiftUI

struct RecordView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("记录")
                    .font(.title)
                    .padding()
                
                Text("暂未实现")
                    .foregroundColor(.gray)
                
                Spacer()
            }
            .navigationTitle("记录")
        }
    }
}

